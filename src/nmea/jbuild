(jbuild_version 1)

(ocamllex (lexer))

(menhir
 ((flags (-v --table))
  (modules (parser))))

(library
 ((name nMEA)
  (public_name nmea)
  (synopsis "NMEA/GP file format (GPS data)")
  (modules (Coordinates Utils GP Sentence Parser Lexer Input))
  (libraries (ptime menhirLib))
  (flags (:standard -warn-error A))))
